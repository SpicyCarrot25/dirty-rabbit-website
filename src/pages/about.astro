---
import Layout from '../layouts/Layout.astro';
import { getTranslations } from '../i18n';

const lang = 'es';
const t = getTranslations(lang);
---

<Layout title={`${t.about.title} — Dirty Rabbit`} lang={lang}>
  <!-- Hero -->
  <section class="py-16 md:py-24 bg-cream">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="reveal font-typewriter text-5xl md:text-6xl font-bold mb-12 text-center">{t.about.title}</h1>
      
      <!-- Our Story -->
      <div class="grid md:grid-cols-2 gap-12 items-center mb-24">
        <div class="reveal">
          <h2 class="font-typewriter text-3xl font-bold mb-6">{t.about.ourStory}</h2>
          <div class="font-mono text-lg text-black/70 leading-relaxed space-y-4">
            <p>[COPY TBD]</p>
          </div>
        </div>
        <div class="reveal">
          <!-- TODO: Replace with real photo -->
          <div class="w-full aspect-[4/3] bg-black/10 flex items-center justify-center font-mono text-sm text-black/40">
            [PHOTO TBD]
          </div>
        </div>
      </div>

      <!-- The Space -->
      <div class="grid md:grid-cols-2 gap-12 items-center mb-24">
        <div class="reveal order-2 md:order-1">
          <!-- TODO: Replace with real interior photo -->
          <div class="w-full aspect-[4/3] bg-black/10 flex items-center justify-center font-mono text-sm text-black/40">
            [PHOTO TBD]
          </div>
        </div>
        <div class="reveal order-1 md:order-2">
          <h2 class="font-typewriter text-3xl font-bold mb-6">{t.about.theSpace}</h2>
          <div class="font-mono text-lg text-black/70 leading-relaxed space-y-4">
            <p>[COPY TBD]</p>
          </div>
        </div>
      </div>

      <!-- Who We Are -->
      <div class="text-center reveal">
        <h2 class="font-typewriter text-3xl font-bold mb-8">{t.about.whoWeAre}</h2>
        <div class="grid md:grid-cols-2 gap-8 max-w-3xl mx-auto">
          <!-- Team member 1 -->
          <div class="text-center">
            <div class="w-48 h-48 mx-auto bg-black/10 rounded-full flex items-center justify-center font-mono text-sm text-black/40 mb-4">
              [PHOTO TBD]
            </div>
            <h3 class="font-typewriter text-xl font-bold">[NAME TBD]</h3>
            <p class="font-mono text-sm text-black/60">[ROLE TBD]</p>
          </div>
          <!-- Team member 2 -->
          <div class="text-center">
            <div class="w-48 h-48 mx-auto bg-black/10 rounded-full flex items-center justify-center font-mono text-sm text-black/40 mb-4">
              [PHOTO TBD]
            </div>
            <h3 class="font-typewriter text-xl font-bold">[NAME TBD]</h3>
            <p class="font-mono text-sm text-black/60">[ROLE TBD]</p>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>
