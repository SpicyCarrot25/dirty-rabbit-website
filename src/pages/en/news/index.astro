---
import Layout from '../../../layouts/Layout.astro';
import { getTranslations } from '../../../i18n';
const lang = 'en';
const t = getTranslations(lang);
---
<Layout title={`${t.news.title} — Dirty Rabbit`} lang={lang}>
  <section class="py-16 md:py-24 bg-cream">
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="reveal font-typewriter text-5xl md:text-6xl font-bold mb-16 text-center">{t.news.title}</h1>
      <div class="text-center font-mono text-black/50 py-16">[NEWS POSTS TBD]</div>
    </div>
  </section>
</Layout>
