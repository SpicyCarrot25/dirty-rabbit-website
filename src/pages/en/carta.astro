---
import Layout from '../../layouts/Layout.astro';
import menuData from '../../data/menu.json';

const lang = 'en';
---

<Layout title="Menu — Dirty Rabbit" lang={lang}>
  <section class="pt-8 pb-12 md:pt-12 md:pb-16 bg-cream">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h1 class="font-typewriter text-5xl md:text-6xl font-bold mb-6">Menu</h1>
      <div class="w-16 h-px bg-red mx-auto mb-8"></div>
      <p class="font-mono text-sm md:text-base text-black/80 leading-relaxed max-w-xl mx-auto">
        We curate everything we serve. Local and organic products from our neighbors, 
        and treasures we love from afar — coffee from Copenhagen, butter from Belgium, 
        matcha from Kyoto. This is our home, and we want to share it with you.
      </p>
    </div>
  </section>

  <section class="py-8 md:py-12 bg-cream">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      
      <!-- COFFEE & CIA -->
      <div id="coffee" class="scroll-mt-20">
        <h2 class="font-typewriter text-5xl md:text-6xl font-bold text-red mb-8 text-center">Coffee & Cia</h2>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-8">Coffee</h3>
        <div class="space-y-4">
          {menuData.coffee.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <span class="font-typewriter text-lg font-bold">{item.name}</span>
                <span class="font-mono text-sm text-black/70 ml-2">{item.desc[lang]}</span>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-10">Not Coffee</h3>
        <div class="space-y-4">
          {menuData.notCoffee.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <span class="font-typewriter text-lg font-bold">{item.name}</span>
                <span class="font-mono text-sm text-black/70 ml-2">{item.desc[lang]}</span>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-10">Iced Drinks</h3>
        <div class="space-y-4">
          {menuData.icedDrinks.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <span class="font-typewriter text-lg font-bold">{item.name}</span>
                <span class="font-mono text-sm text-black/70 ml-2">{item.desc[lang]}</span>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
        
        <p class="font-mono text-sm text-black/70 text-center mt-8 pt-4 border-t border-black/10">
          Oat or pea milk: +€0.30 · Extra shot: +€0.50
        </p>
      </div>

      <!-- DRINKS -->
      <div id="bebidas" class="scroll-mt-20 mt-16 pt-16 border-t border-red">
        <h2 class="font-typewriter text-5xl md:text-6xl font-bold text-red mb-8 text-center">Drinks</h2>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-8">Smoothies</h3>
        <div class="space-y-5">
          {menuData.smoothies.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <h4 class="font-typewriter text-lg font-bold">{item.name}</h4>
                <p class="font-mono text-sm text-black/70 mt-1">{item.desc[lang]}</p>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
        <p class="font-mono text-xs text-black/60 mt-4">
          Add-ons: Protein +3 · Creatine +1 · Spirulina +1 · Collagen +1.50
        </p>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-10">Beverages</h3>
        <div class="space-y-4">
          {menuData.beverages.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <span class="font-typewriter text-lg font-bold">{item.name}</span>
                <span class="font-mono text-sm text-black/70 ml-2">{item.desc[lang]}</span>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-10">Adult Drinks</h3>
        <div class="space-y-4">
          {menuData.adultDrinks.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <span class="font-typewriter text-lg font-bold">{item.name}</span>
                <span class="font-mono text-sm text-black/70 ml-2">{item.desc[lang]}</span>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
      </div>

      <!-- FOOD -->
      <div id="food" class="scroll-mt-20 mt-16 pt-16 border-t border-red">
        <h2 class="font-typewriter text-5xl md:text-6xl font-bold text-red mb-8 text-center">Food</h2>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-8">Toasts & Toasties</h3>
        <div class="space-y-5">
          {menuData.toasts.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <h4 class="font-typewriter text-lg font-bold">{item.name}</h4>
                <p class="font-mono text-sm text-black/70 mt-1">{item.desc[lang]}</p>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
        <p class="font-mono text-xs text-black/60 mt-4">
          Extras: Iberian Ham +3 · Salmon +3 · Egg +2 · Manchego +2.50 · Avocado +2.50
        </p>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-10">Bagels</h3>
        <div class="space-y-5">
          {menuData.bagels.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <h4 class="font-typewriter text-lg font-bold">{item.name}</h4>
                <p class="font-mono text-sm text-black/70 mt-1">{item.desc[lang]}</p>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-10">Bowls</h3>
        <div class="space-y-5">
          {menuData.bowls.map((item) => (
            <div class="flex items-baseline justify-between gap-4">
              <div class="flex-1 min-w-0">
                <h4 class="font-typewriter text-lg font-bold">{item.name}</h4>
                <p class="font-mono text-sm text-black/70 mt-1">{item.desc[lang]}</p>
              </div>
              <span class="font-typewriter text-lg font-bold text-red tabular-nums shrink-0">{item.price}</span>
            </div>
          ))}
        </div>
        
        <h3 class="font-typewriter text-2xl font-bold text-red mb-4 mt-10">Sweet Bites</h3>
        <p class="font-mono text-sm text-black/70">
          Check our counter for today's selection of cookies, croissants and cakes.
        </p>
      </div>

      <div class="mt-16 pt-8 border-t border-black/10">
        <p class="font-mono text-sm text-black/70 text-center">
          Ask about allergens — kitchen is not 100% gluten-free
        </p>
      </div>
    </div>
  </section>

  <nav class="fixed bottom-0 left-0 right-0 bg-cream border-t border-red py-5 z-50 md:hidden safe-area-bottom">
    <div class="flex justify-center items-center">
      <a href="#coffee" class="flex-1 flex items-center justify-center font-typewriter text-xl font-bold text-red border-r border-red h-14">Coffee & Cia</a>
      <a href="#bebidas" class="flex-1 flex items-center justify-center font-typewriter text-xl font-bold text-red border-r border-red h-14">Drinks</a>
      <a href="#food" class="flex-1 flex items-center justify-center font-typewriter text-xl font-bold text-red h-14">Food</a>
    </div>
  </nav>
  <div class="h-20 md:hidden"></div>
</Layout>

<style>
  .tabular-nums { font-variant-numeric: tabular-nums; }
  html { scroll-behavior: smooth; }
  .safe-area-bottom { padding-bottom: max(1rem, env(safe-area-inset-bottom)); }
</style>
